   {{name}}:
    type: object
    properties:
    {% for key in api.requestBody %}
      ams:code:
        type: {{key}}
        example: {{api.requestBody[key]|get_type}}
    {% endfor %}
    xml:
      prefix: '{{prefix}}'
      namespace: '{{namespace}}'